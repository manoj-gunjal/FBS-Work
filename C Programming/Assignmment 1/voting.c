// Write a c program to check whethe a person is eligible to vote or not.

#include<stdio.h>
void main() {
    int age = 19;
    if(age>=18) {
        printf("Person is eligible to vote....");
    } else {
        printf("person not eligible to vote....");
    }
}