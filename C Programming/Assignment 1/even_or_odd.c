// Write a c program to check whether number is even or odd.

#include<stdio.h>
void main() {
    int num = 15;
    if (num%2==0) {
        printf("%d is even",num);
    } else {
        printf("%d is odd",num);
    }
}